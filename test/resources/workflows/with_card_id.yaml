jobs:
  example:
    steps:
      - uses: RandomOrg/RandomName
        with:
          one: 1
          two: 2
      - uses: ActiveEngagement/theguru@master
        with:
          file_path: some/random/path
          card_id: initialCard123